# Pre-Generation Eval Report

Status: pass pending prompt approval.

Overall score: 4.6 / 5

The prompt pack is ready because it:

- Keeps Aachu as the asker and Zuv as the receiver.
- Preserves the exact source line with the intended two-line layout.
- Uses native 4:5 and native 9:16 prompt variants.
- Specifies actual local identity-reference roles.
- Rejects role reversal, face drift, face merge, yellow paper, missing brandmark, and quote-card drift.

Remaining gate before imagegen:

Creator must approve the prompt lock. After approval, selected identity and style references must be loaded with `view_image`, then built-in Codex `imagegen` can generate one surface at a time.
